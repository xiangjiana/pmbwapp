name: banner # $group/$name_$method.dart
group: config # $group_apis.dart
method: post # [get, post]
uri: /config/banner # uri

# 入参序列化使用，此处将会自动生成对应的 model，并实现 fromJson toJson 方法。model 名为 req_xxx_model.dart
req: { "clientType": "laboris labore veniam consequat" }
# 指定数据对象 json，自动生成 response 的对象
# 默认创建 response 序列化使用的 adapter，用于数据序列化的手动操作。默认为直接根据 json 序列化，不存在该文件则创建，否则忽略。
resp:
  {
    "data":
      [
        {
          "areaType": 0,
          "bannerName": "string",
          "bannerTime": "string",
          "bannerValidity": 0,
          "clientType": 0,
          "downTime": "string",
          "gameCode": "string",
          "id": "string",
          "isLink": 0,
          "linkTarget": 0,
          "merchantId": "string",
          "pictureUrl": "string",
          "sort": 0,
          "status": 0,
          "targetUrl": "string",
          "upTime": "string",
        },
      ],
  }
