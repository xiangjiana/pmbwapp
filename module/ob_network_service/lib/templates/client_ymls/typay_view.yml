name: depositview # $group/$name_$method.dart
group: typay # $group_apis.dart
method: post # [get, post]
uri: /typay/deposit/view # uri

# 入参序列化使用，此处将会自动生成对应的 model，并实现 fromJson toJson 方法。model 名为 req_xxx_model.dart
req: { "merchant_id": 0, "merchant_order_id": "string", "remark": "string" }
# 指定数据对象 json，自动生成 response 的对象
# 默认创建 response 序列化使用的 adapter，用于数据序列化的手动操作。默认为直接根据 json 序列化，不存在该文件则创建，否则忽略。
resp:
  {
    "bankCode": "string",
    "merchantId": 0,
    "merchantOrderId": "string",
    "payAmt": 0.0,
    "payMessage": 0,
    "payPaidAmt": 0.0,
    "payResult": "string",
    "payType": 0,
    "remark": "string",
    "sign": "string",
    "typayOrderId": "string",
    "userPaidCardno": "string",
    "userPaidName": "string"
  }
