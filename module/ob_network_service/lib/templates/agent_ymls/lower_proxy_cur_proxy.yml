name: lowerProxyCurProxy # $group/$name_$method.dart
group: proxy # $group_apis.dart
method: post # [get, post]
uri: /lowerProxy/curProxy # uri

# 入参序列r化使用，此处将会自动生成对应的 model，并实现 fromJson toJson 方法。model 名为 req_xxx_model.dart
req: { "orderType": true, "userName": "test000" }
# 指定数据对象 json，自动生成 response 的对象
# 默认创建 response 序列化使用的 adapter，用于数据序列化的手动操作。默认为直接根据 json 序列化，不存在该文件则创建，否则忽略。
resp:
  {
    "createDt": "string",
    "directlyUnderMemberNum": 0,
    "directlyUnderProxyNum": 0,
    "id": "string",
    "lastLoginTime": "string",
    "offLineDays": 0,
    "proxyPathName": "string",
    "registerType": 0,
    "underMemberNum": 0,
    "underProxyNum": 0,
    "userName": "string",
  }
