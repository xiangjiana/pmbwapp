name: proxyProxyInfo # $group/$name_$method.dart
group: proxy # $group_apis.dart
method: post # [get, post]
uri: /proxy/proxyInfo # uri

# 入参序列化使用，此处将会自动生成对应的 model，并实现 fromJson toJson 方法。model 名为 req_xxx_model.dart

# 指定数据对象 json，自动生成 response 的对象
# 默认创建 response 序列化使用的 adapter，用于数据序列化的手动操作。默认为直接根据 json 序列化，不存在该文件则创建，否则忽略。
resp:
  {
    "accountType": 0,
    "commissionBalance": 10.00,
    "contractModel": 0,
    "createAt": "string",
    "flyGram": "string",
    "headImgUrl": "string",
    "maxLevels": 0,
    "parentProxyId": 0,
    "payPasswordFlag": 0,
    "proxyId": 0,
    "proxyLevelId": 0,
    "proxyName": "string",
    "qq": "string",
    "quotaBalance": 10.00,
    "signContractType": 0,
    "unReadMsgCount": 0,
    "unReceiveRebateCount": 0
  }
